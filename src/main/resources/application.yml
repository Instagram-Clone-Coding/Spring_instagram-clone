spring:
  datasource:
    url: jdbc:mysql://database:3306/instagram?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
      properties:
        hibernate:
          format_sql: true
          default_batch_fetch_size: 100
          dialect: org.hibernate.dialect.MySQL5Dialect
    open-in-view: false

  security:
    oauth2:
      client:
        registration:
          facebook:
            client-id: 274825254265205
            client-secret: 691a349a8970d2566b8effb15cd74638
            scope:
              - email
              - public_profile
logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace